# Changelog

All notable changes to this project will be documented in this file.

## [0.28.0.0] - 2026-06-30

### Added
- **선거주기(연도) UI 분리 — 옛 주기 사용기관 읽기전용 잠금**: 한 사용자가 2022·2026처럼 여러 선거주기 기관을 함께 쓸 때, 옛 주기(과거 연도) 기관은 실수 입력을 막기 위해 **읽기전용**으로 잠근다. 수입·지출·문서등록·일괄등록 입력 페이지 상단에 *"🔒 옛 선거주기 사용기관입니다 — 읽기전용"* 배너가 뜨고, [잠금 해제]를 눌러야 입력·저장이 가능하다(감사 정정용). 대시보드 사이드바의 기관 정보에도 옛 주기일 때 자물쇠 표식이 표시된다.
- **사용기관 선택 화면 선거주기 배지·필터**: 사용기관 선택 목록에 각 기관의 선거주기 배지(`2026년 주기` / 옛 주기는 `· 옛`)를 표시한다. 기본은 **현 주기만** 보여 주고, 옛 주기 기관이 있으면 *"옛 주기 사용기관 N곳 보기"* 토글로 펼칠 수 있다. 현 주기 기관이 하나도 없으면 안내 문구를 띄운다.

> 선거주기 판정은 `lib/accounting/acc-period.ts`(현 주기 = 사용자 기관 중 최신 주기, 옛 주기 = 그보다 과거)를 단일 출처로 쓰며, 데이터 무결성 1차 방어는 0.27.0.0의 입력 시점 서버 검증이 계속 담당한다(잠금은 UX 가드).

## [0.27.0.0] - 2026-06-30

### Added
- **연도(선거주기) 혼입 입력 가드**: 거래(수입·지출·문서등록·일괄입력) 저장 시, 거래일이 해당 사용기관의 회계기간 밖이면 *"거래일이 회계기간 밖입니다. 그래도 저장하시겠습니까?"* 확인을 띄운다. 취소하면 저장이 막히고, 확인하면 그대로 저장된다. 2022 기관에 2026 거래가 잘못 들어가는 식의 연도 혼입을 입력 시점에 차단한다.
- **사용기관 선거주기 컬럼(`election_cycle`)**: 사용기관마다 선거주기(연도)를 명시 저장하는 컬럼을 추가했다(마이그레이션 `scripts/021`, 수동 적용). 회계기간 시작 연도로 자동 채워진다.
- **연도 혼입 점검 스크립트**: `scripts/scan-year-contamination.mjs`로 전체 사용기관의 기간 밖 거래를 한 번에 점검할 수 있다(정기 점검용).

### Fixed
- 2022 사용기관에 잘못 들어가 있던 2026 거래 1건(후원금 40만원, 중복)을 정리해 연도 데이터를 분리했다.

### Added
- **윈도우 [자료 복구]용 단일기관 백업 (자료백업 신규 형식)**: 자료백업 화면에 "윈도우 [자료 복구]용 (단일기관)" 형식을 추가했다. 후보자·후원회처럼 **각각 별도 사용기관(별도 로그인)** 으로 운영되는 회계를, 선관위 윈도우 프로그램이 자기 백업처럼 받아들이는 **단일기관 보관자료(.db) 스냅샷**으로 내려받아 프로그램 **[자료 복구]** 메뉴로 적재할 수 있다. 파일명도 프로그램 형식(`정치자금【기관명】보관자료_날짜시간.db`)으로 자동 생성된다.
- **프로그램 기관번호(ORG_ID) 자동 확인**: 윈도우 프로그램은 기관마다 고유 번호(ORG_ID)를 순차 부여하고 [자료 복구]는 그 번호 그대로 적재하므로(번호가 다르면 충돌해 복구 실패), 사용자가 프로그램의 `Fund_Master.db`를 올리면 그 안에서 이 기관의 실제 번호를 읽어 백업에 그대로 반영한다(서버에 저장하지 않고 브라우저에서 읽음). 자동 인식이 애매하면 드롭다운으로 직접 선택할 수 있다.

### Changed
- 자료백업의 단일기관 모드는 후원회를 내보낼 때 **가짜 후보자 기관을 만들지 않는다**(후보자를 별도 기관으로 운영하는 사용자 환경에 맞춤). Data 폴더에 직접 붙여넣지 말고 [자료 복구] 메뉴로, 해당 기관으로 로그인한 상태에서 그 기관 파일만 복구하도록 화면에 안내한다.

## [0.25.1.0] - 2026-06-29

### Fixed
- **지방선거 후원회(광역·기초의회) 자료백업 ORG_ID 오배정 수정**: 광역/기초의회의원 후원회(`org_sec_cd` 593·594·595·596)를 자료백업하면, 윈도우 선관위 프로그램에서 ① 사용기관 목록에 후원회만 뜨고 후보자 기관이 안 보이며 ② 후원회로 로그인해도 후보자 기관의 수입·지출 내역이 표시되던 문제를 해소. 원인은 후원회 분류 집합(`SUPPORTER_SEC_CDS`)에 지방선거 후원회 코드 4종이 빠져 있어, 후원회가 후보자 자리(ORG_ID=1)로 단독 export되고 거래도 `Fund_Data_1`(후보자)로 매핑된 것. 이제 후원회는 ORG_ID=2 + 후보자 페어(ORG_ID=1)로 정상 분리되어 마스터 목록·로그인 내역이 올바르게 표시된다. (`import-sqlite`에 중복돼 있던 동일 분류 집합도 공유 SSOT로 일원화해 재발을 막았다.) **적용하려면 코드 배포 후 자료백업을 다시 받아야 한다.**

## [0.25.0.0] - 2026-06-26

### Changed
- **수입 영수증 일련번호 "생략" 표기**: 수입·지출부의 모든 산출물(화면 뷰어·과목별 정치자금 수입지출부 Excel·개별 수입부 Excel·회계장부/수입지출부 HWPX·선관위 제출 파일)에서 수입 거래의 영수증 일련번호를 공식 양식과 동일하게 **"생략"**으로 표기한다. 지출만 실제 영수증번호(예: `자(비)-1`)를 받는다. 기존에는 수입에도 번호가 부여돼 선관위 윈도우 프로그램·인쇄 양식과 어긋났으나, 이제 공식 표본(`Fund_Data_1.db`·인쇄 PDF)과 1:1로 일치한다.
- **영수증 채번 로직 단순화(지출 전용 스코프)**: 채번 SSOT(`fillExportReceiptNumbers`)가 수입을 채번 대상에서 제외하고, 자료백업(.db) export 시 수입 영수증번호를 빈값으로 내보내 공식 포맷(수입 `RCP_NO` 빈값)과 정합한다.

### Removed
- **수입 페이지 「영수증일괄입력」 버튼**: 수입은 영수증 일련번호를 생략하므로 입력 경로를 제거했다(`batch_receipt` API도 수입 요청은 무시).

## [0.24.1.0] - 2026-06-24

### Fixed
- **결산작업 계정/과목별 차액에 자금원 재배분 적용**: 결산작업 화면이 원본 거래를 그대로 집계해, 후보자 자금원별로 (현실에선 불가능한) 음수 차액(예: 후원회기부금 −126,517 ↔ 후보자등자산 +126,517 상쇄)이 표시되던 문제를 해소. 이제 결산 집계도 수입·지출부·수입지출보고서(서식 22-1)·.db 백업과 **동일한 재배분 SSOT**(`allocateCandidateLedgerRows`)를 거쳐, 한 자금원에서 받은 것보다 더 쓴 것처럼 보이던 지출이 실제 충당 자금원으로 정리된다. 재배분은 자금원별 수입·지출 합과 전체 잔액(수입−지출)을 보존하므로 **잔액=현금및예금 검증과 결산확정에는 영향이 없다**. 더불어 재배분으로도 메울 수 없는 통장 잔액 부족(실제 데이터 오류)은 은폐하지 않고 별도 경고로 표시한다.

## [0.24.0.0] - 2026-06-24

### Changed
- **자금원 재배분 시 소액 자투리 조각 방지("단독 충당 우선")**: 후보자 지출을 자금원별로 충당할 때, 부족분을 **단독으로 전부 덮을 수 있는 자금원이 있으면 그 자금원으로 한 번에 충당**하도록 개선. 기존에는 우선순위 1순위(후보자등자산)에 가용액이 조금이라도 있으면 부분 충당으로 먼저 끌어써, 1~수십 원짜리 자투리 지출 조각(예: 인형탈대여 충당 중 후보자등자산 58원)이 별도 행·영수증번호로 생겼다. 이제 부족분 전체를 댈 수 있는 자금원(예: 잔액이 충분한 보조금외)으로 통째 충당하고, 소액 자금원의 잔액은 그 자금원의 후속 지출에서 정산된다. 총 지출액·자금원별 잔액(≥0)·충당 우선순위 불변식은 그대로 유지된다(화면·Excel·HWPX·.db 백업 공통 SSOT). 자투리가 단독으로 못 덮는 경우에는 기존처럼 여러 자금원으로 분할한다.

## [0.23.0.0] - 2026-06-24

### Added
- **감사의견서에 재산·수입·지출·잔액을 자동 표기**: 감사의견서 "나. 감사대상"에 재산상황 금액과 정치자금 수입·지출·잔액을 표시한다. 감사의견서 화면에 들어가면 결산이 자동으로 재계산(`recompute-settlement` = 보고서·총괄표와 동일한 `settlement-calc` SSOT)되어 `opinion`의 금액이 최신화되므로, 사용자가 금액을 직접 입력하지 않아도 자동으로 표기된다. (심사의결서 탭에서 그 자리에서 수정해 출력하는 것도 가능. 자동 재계산이 실패해도 저장된 값으로 표기되어 출력은 막히지 않는다.)

## [0.22.0.0] - 2026-06-24

### Added
- **감사의견서에 감사(감사자)를 최대 5명까지 입력·출력**: 정당·후원회는 정관·당규에 따라 감사를 2명 이상 두는 경우가 많고 공식 감사의견서 양식도 감사자 전원을 서명란에 기재할 수 있는데, 기존에는 1명(직위·주소·성명)만 기재할 수 있었다. 감사의견서 입력 화면에 감사자 1~5번 칸을 추가하고, 출력 시 값이 입력된 감사자만 (직위/주소/성명/(인)) 서명 블록으로 반복 표시한다. 회계기간·감사기간·감사의견 등 공통 정보는 종전과 동일.

### Changed
- 자료백업(.db) 생성 시, 선관위 공식 PFund2 포맷에는 감사자 칸이 1명분뿐이므로 **1번 감사자만 .db에 내보낸다**(2~5번 감사자 컬럼은 export 직전 제거 — `claim_amt`·`acc_time`과 동일한 app-only 컬럼 처리). 화면 출력과 DB에는 감사자 전원이 보존된다. (DB 마이그레이션 `scripts/020` 적용 필요.)

## [0.21.3.0] - 2026-06-23

### Fixed
- **지출내역 관리 화면의 자금원별 충당 현황이 보고서·총괄표와 다르던 문제 수정**: 후보자 잔액이 0(또는 ≥0)이 맞는데도 일부 자금원이 마이너스(과지출)로 표시되던 불일치를 해결. 패널 집계를 보고서·수입지출부·자료백업(.db)과 **동일한 재배분 기준**으로 전환해, 과지출 자금원의 초과분이 잔여 자금원으로 옮겨간 결과(가용잔액 ≥ 0)를 보여준다. 이제 지출내역 화면의 자금원별 금액이 보고서와 일치한다. 패널에 "재배분 반영" 라벨 추가. 전체 수입/지출/잔액 요약과 비후보자 화면은 변동 없음. (입력 시점 실시간 자금원 추천 미리보기는 입력 가이드 목적상 기존 그대로 유지.)

## [0.21.2.0] - 2026-06-23

### Fixed
- **제출파일(.db)을 선관위 윈도우 프로그램에서 인쇄 시 영수증일련번호 뒤에 숫자가 덧붙던 문제 수정** (예: `자(비)-24`가 `자(비)-2467`로 출력). 공식 프로그램은 영수증번호를 **접두사(`RCP_NO`, 예 "자(비)-")와 숫자(`RCP_NO2`, 예 24)로 나눠 저장하고 인쇄 시 둘을 이어붙여 표시**하는데, 우리 백업은 전체 번호("자(비)-24")를 접두사 칸에 넣고 숫자 칸에는 내부 정렬용 일련번호(예 67)를 넣어, 프로그램이 "자(비)-24"+"67"로 합쳐 표시했다. 실제 공식 Fund_Data 파일의 저장 형식을 확인해, 백업 생성 시 영수증번호를 접두사/숫자로 분리하도록 정정. 영수증번호가 없는 행은 숫자 칸을 0으로 둬 덧붙음을 방지. (CUSTOMER 컬럼 오류 수정 후 후보자 .db가 정상 로드되며 드러난 문제.)

## [0.21.1.0] - 2026-06-23

### Fixed
- **제출파일(.db) 생성 오류 `table ACC_BOOK has no column named CUSTOMER` 수정**: 후보자 자료백업(.db) 생성 시 재조정(분할) 과정에서 거래처 조인 정보(`customer`)가 보고용 장부 행에 따라붙어, 공식 ACC_BOOK 형식에 없는 컬럼으로 새어 들어가 백업 전체가 실패하던 문제를 해결. 해당 임시 필드를 export 직전에 제거하도록 정정(`acc_time` 제거 후 드러난 같은 부류의 잠복 결함). 재발 방지 회귀 테스트 추가. 후원회 등 비후보자 백업은 영향 없음.

## [0.21.0.0] - 2026-06-23

### Fixed
- **보고서·과목별 수입·지출부(Excel) 영수증일련번호 정합**: 후보자 보고서를 출력할 때 자금원 재배분으로 분할·이동된 지출 조각이 **원본 영수증번호를 그대로 물려받아**, 같은 번호(예 `후(비)-10`)가 서로 다른 자금원 시트에 **중복** 출력되고 접두사가 시트의 자금원과 어긋나던 문제를 해결. 이제 영수증번호를 자료백업(.db)·재조정 데이터 뷰어와 **동일한 채번 SSOT**(`fillExportReceiptNumbers`)로 산출해, 이동 조각은 옮겨간 자금원 접두사로 재부여되고 시트별로 고유한 번호가 매겨진다. 화면·Excel·.db가 같은 번호를 보장(원본 acc_book 데이터는 불변, 출력 시점에만 계산). 후보자 기관에만 적용되며 비후보자 보고서는 영향 없음.

### Added
- **자료백업(.db) 수입·지출 분할 계약 검증**: `.db` 생성 시 보고용 장부(`ACC_BOOK`)는 수입·지출을 자금원별로 **분할(재배분)**한 데이터로, 변경이력 백업(`ACC_BOOK_BAK`)은 **원본** 그대로 구성한다는 비대칭 계약을 회귀 테스트로 고정하고 문서화. 이미 동작하던 분할 동작이 향후 변경으로 깨지면 CI가 사전 차단한다.

## [0.20.1.0] - 2026-06-23

### Fixed
- 정치자금 수입·지출부의 **영수증일련번호 중복 제거**: 재조정(이동·분할) 생성본에서 같은 접두사(`자(비)`·`후(비)`·`외(비)` 등)가 수입행과 지출행에 각각 1번부터 매겨져 동일 번호가 2회 이상 나타나던 문제(실데이터 20건)를 해결. 채번을 **수입·지출 통합 단일 스코프**로 전환하고, 각 행의 현재 (계정×과목) 기준으로 번호를 부여하도록 정정. 자금원 재배분으로 다른 계정으로 옮겨진 거래 조각이 원본 접두사를 그대로 달고 있던 불일치(5건)도 현재 계정 접두사로 자동 교정. 기존에 손으로 매긴 영수증번호는 그대로 보존하며, 원본 장부(acc_book) 데이터는 변경하지 않는다(보고/백업 생성 시점에만 채번). 화면(재조정 데이터 뷰어)과 자료백업(.db)이 동일 번호를 산출함을 교차 검증.

## [0.20.0.1] - 2026-06-23

### Removed
- 시분초 제거 마무리(Phase 3): `acc_time` 컬럼이 DB에서 DROP(`019` 적용)된 뒤, 자료백업(export-sqlite)의 app-only 컬럼 strip 목록에서 더 이상 존재하지 않는 `acc_time` 항목을 제거하고 관련 테스트를 실재 컬럼(claim_amt·alloc) 기준으로 정리. 동작 영향 없음(strip은 이미 no-op이었음).

## [0.20.0.0] - 2026-06-22

### Removed
- 거래 시각(시·분, `acc_time`) 입력·표시를 전면 제거. 수입·지출·문서등록 화면의 「거래 시각」 입력 흔적이 사라지고, 거래는 날짜 단위로만 기록·정렬한다. (시분초는 사용하지 않기로 한 결정 — `acc-time-input`(v0.7.0.0) 되돌림. `scripts/019_drop_acc_time.sql`로 컬럼 DROP, 코드 배포 후 적용.)

### Changed
- 같은 날 거래의 정렬·영수증 채번을 `거래일 → 수입 먼저 → 입력순(acc_book_id)` 기준으로 일관화. 화면·HWPX·Excel·SQLite 백업 산출물의 같은 날 순서·수치는 동일하게 유지(회귀 0). 자금원 잔액 가드의 시점 잔액은 날짜 단위로 단순화.

## [0.19.1.0] - 2026-06-22

### Fixed
- 정치자금 수입지출부 페이지 조회 실패(`column acc_book.acc_time does not exist`) 수정. 시분초(거래 시각)를 사용하지 않기로 한 결정에 따라, 직전 변경에서 들어왔던 `acc_time` 컬럼 조회·정렬 의존을 제거. 같은 날 거래는 `acc_book_id` 순으로 채번되며 자료백업(.db)과 동일하게 유지된다.

## [0.19.0.0] - 2026-06-22

### Added
- 재조정 데이터 뷰어(정치자금 수입지출부): 보고용으로 재조정된 (자금원×과목) 장부를 한 화면에서 검토. 각 행에 「구분」(원본/🔀이동/✂분할) 컬럼과 「재배분 {원}→{현}」 비고로 분할·이동 근거를 표시하고, 통장 잔액 부족이 있으면 빨강 경고 배너로 노출. 제목 옆 「보고용 재조정 데이터 · 원본 불변」 배지로 원본 입력과 구분.
- 영수증 일련번호 일괄생성: 재조정 행 기준으로 영수증번호를 결정적으로 계산해 표시(원본 acc_book 미변경, DB write 0). 화면에서 본 번호가 SQLite 백업(.db)·HWPX·Excel 산출물과 100% 일치하도록 보장.

### Changed
- 재조정 산출 로직을 단일 SSOT(`buildAdjustedAccBook`)로 추출해 수입지출부 화면과 자료백업(export-sqlite)이 같은 함수를 공유 — 화면 == .db 정합을 구조적으로 보장. 분할/이동된 거래 조각에 고유 식별자를 부여해 영수증 채번·화면 표시(React key) 안정화.

## [0.18.0.0] - 2026-06-21

### Added
- 통장 잔액 부족(데이터 오류) 경고: 수입·지출보고서 화면에 자금원 배정 불가 행이 있으면 자금원·일자·금액과 함께 빨강 배너로 표시하고, 회계보고서·회계장부 HWPX 응답에 부족 건수 경고를 노출(거래 상세는 서버 로그만). 더 이상 통장 부족이 0/음수로 조용히 묻히지 않음.
- 자금원 배정 방식 단일 권위 문서(`docs/05-reference/자금원배정방식.md`) — 배정 규칙·전제·소비처·결정 기록 일원화.

### Changed
- 자금원(수입계정) 배정 결과가 화면·HWPX·SQLite 백업 등 모든 출력물에서 동일하도록 단일 배정 로직(SSOT)을 전 경로에 강제. 자료백업의 후보자 자금원 판정을 공용 기준으로 통합.

### Removed
- 미사용 데드 라우트 `/api/excel/report`와 그 전용 Excel 템플릿 체인(13파일) 제거 — 화면 간 수치 불일치를 유발하던 SSOT 우회 경로 제거 및 공격 표면 감소.

### Fixed
- 신규 보고서 출력 경로가 배정 로직을 우회해 수치가 어긋나는 표류를 교차검증 회귀 테스트로 구조적 차단.

## [0.17.0.0] - 2026-06-20

### Changed

- **「정치자금 수입지출보고서」 화면·엑셀이 공식 회계보고서(서식 22-1)와 동일 기준으로 집계됩니다.** 페이지가 선거비용을 과목 코드(86·19)로 하드코딩하고 자금원을 이름으로 추정하던 자체 집계를, 정규 배분 SSOT(`buildCandidateReportSummary` → buildLedgerRows + buildReportSummaryModel)로 일원화했습니다. 같은 데이터면 HWPX 22-1과 수치가 일치하고, 보조금/보조금외가 분리되며, 음수 수입 보정이 화면 안내와 정합합니다. 폐기된 '자금출처 재배분(Rule 2)' 토글은 제거했습니다.

### Added

- **개발/운영 참고 문서 「정치자금 수입·지출부 생성 주의사항」**(`docs/05-reference/`) — 잔액이 깨지는 원리, 보고 시점 분할 모델, 선거비용=과목(item_sec_cd) 분류 SSOT, 라운드트립 정렬, 제출 전 검증 체크리스트. 감사로 발견한 정규 배분 미준수 경로(V1 해소 / V2·V3 잔여)도 §12에 기록.

## [0.16.0.0] - 2026-06-20

### Added

- **정치자금 수입·지출부가 계정(자금원)·과목별로 잔액이 음수 없이 출력됩니다.** 후보자 회계에서 수입의 과목(선거비용/선거비용외)이 실제 집행과 어긋나 (계정×과목)별로 뽑으면 잔액이 음수가 되던 문제를, 공식 프로그램과 동일하게 **수입을 충당한 지출의 과목으로 자동 배분**하여 해소했습니다. 수입·지출부 화면·엑셀 리포트·회계장부/회계보고서(HWPX 서식7·22-1·22-2·22-4)·선관위 SQLite 백업 모두에 적용됩니다.

### Changed

- **분할은 보고자료를 만들 때만 적용되고, acc_book(실거래 데이터)은 원본 그대로 유지됩니다.** "후원회기부금에서 100,000원 지출" 같은 실제 거래는 데이터에서 쪼개지 않고 사실 그대로 보관하며, 자금원별/과목별 보고서를 만들 때만 금액을 배분해 표시합니다.
- **선거비용/선거비용외 분류를 모든 출력물에서 과목(item_sec_cd) 기준으로 통일했습니다.** 총괄표·대시보드 선거비용 카드·결산 집계가 지출유형(미입력 많음) 대신 과목으로 분류해, 지출유형을 입력하지 않아도 선거비용이 0으로 빠지지 않습니다.
- **거래 시각(시·분) 입력을 제거했습니다.** 같은 날 수입을 지출보다 먼저 배치하는 정렬은 자동으로 처리되며(수입 우선 + export 정렬번호 재부여), 시각은 더 이상 저장·출력하지 않습니다.

### Fixed

- **선관위 윈도우 프로그램으로 백업을 불러왔을 때 수입·지출부 누계가 일시 음수로 표시되던 문제를 수정했습니다.** export 시 모든 거래에 정렬번호(acc_sort_num)를 정규 순서로 재부여해, 시각 정보 없이도 같은 날 수입이 지출보다 먼저 렌더링됩니다.

## [0.15.1.0] - 2026-06-19

### Reverted

- **정치자금 수입·지출부에 자금원 재배분을 적용했던 0.15.0.0을 되돌렸습니다.** 0.15.0.0은 과목으로 뽑을 때 "수입은 자금원 전체, 지출은 선택 과목"만 표시(Option B)했는데, 후보자 수입의 과목 태그가 사실상 무의미해(대부분 선거비용 과목으로 기장) 선거비용외정치자금을 뽑으면 무관한 거액 수입까지 합산되어 **잔액이 비정상적으로 부풀려지는 문제**가 있었습니다. 우선 0.14.8.0 동작으로 되돌립니다. 선거비용외 과목 음수 잔액 해소는, 잔액을 과목이 아닌 **자금원(계정) 단위로 계산하는 올바른 모델**로 별도 재작업할 예정입니다.

## [0.14.8.0] - 2026-06-18

### Added

- **「일괄출력(보고서)」 정치자금 수입·지출부 엑셀에 거래가 없는 표준 계정·과목도 빈 양식으로 출력됩니다.** 이전에는 그 회계기간에 거래가 한 건이라도 있는 계정×과목 조합만 시트로 만들어, 거래가 0건인 표준 계정(예: 후보자 보조금)은 시트 자체가 누락됐습니다. 이제 기관유형의 **표준 계정×과목(acc_rel) 전체**를 기준으로 시트를 만들어, 거래가 있으면 내역을, 없으면 **손기입용 빈 행 1개의 빈 양식**을 출력합니다(계정표지·과목표지 포함). 화면에서 체크한 계정/과목 범위는 그대로 존중하며(선택하지 않은 항목은 빈 시트도 만들지 않음), 거래가 있는 시트의 내역·합계·정렬은 종전과 동일합니다. 선관위 제출 시 표준 계정 누락 위험을 없애 양식 완전성을 확보합니다. (이미 빈 계정을 출력하던 HWPX 서식7의 검증된 패턴을 엑셀에 이식)

## [0.14.7.0] - 2026-06-17

### Added

- **(예비)후보자 지출 입력 시 「자금원 잔액 가드」가 추가됩니다.** 지출관리·서류등록(수기입력) 화면에서 지출을 입력할 때, **입력하려는 거래일 기준**으로 자금원(후보자자산·후원회기부금·보조금·보조금외)별 가용잔액과 **"이 지출을 저장하면 잔액이 얼마가 되는지"(저장 후 예상)** 를 실시간으로 보여줍니다. 선택한 자금원이 음수가 되면 경고하고 **여유 있는 다른 자금원을 권장**합니다. 이때 음수는 통장 현금 부족이 아니라 "이 자금원으로 태깅하면 수입지출부·결산에서 음수가 되어 **사후 재배분이 필요**"하다는 뜻이며, **저장은 막지 않습니다**(참고용 안내). 입력 거래일까지 입금된 금액만 시간순으로 반영(`acc-book-sort` 정렬 SSOT 재사용)하므로, 실제 자금 흐름에 맞춰 자금원을 고를 수 있어 수입지출부 음수 잔액을 입력 단계에서 예방합니다. (후보자 전용, 환급(음수 지출)·수정 시 자기 행 제외 정확 반영)

## [0.14.6.0] - 2026-06-16

### Fixed

- **정치자금 수입·지출부에서 같은 날 거래가 입력한 시각 순서대로 정렬됩니다.** 이전에는 거래 시각(`acc_time`)이 입력은 되어도 장부·목록 정렬에 반영되지 않아, 같은 날 새벽에 받은 수입과 오후에 쓴 지출이 입력 순서·계정 구분에 따라 뒤바뀌어 표시되곤 했습니다(예: 지출이 수입보다 먼저 표시). 이제 **거래일 → 거래 시각** 순으로 정렬하는 단일 기준(SSOT)을 모든 장부·목록에 적용합니다 — 정치자금 수입·지출부(엑셀), 수입지출부 조회 화면, 지출관리, 보전 수입·지출부, 선관위 HWPX 회계장부(서식 7·22-4), 개별 수입부/지출부, 지출결의서가 모두 동일하게 동작합니다. 시각이 같거나 미입력된 거래는 종전처럼 **수입을 먼저** 표시해 잔액이 음수로 떨어지지 않습니다. (영수증번호 채번 순서와 첨부서류 목록은 변경되지 않습니다.)

## [0.14.5.0] - 2026-06-16

### Changed

- **영수증번호 채번 규칙이 선관위 공식 표기 예시에 맞춰 정비됩니다.** 「영수증 일괄생성」과 자료백업(.db) 모두 동일 SSOT를 쓰며, 계정 계열에 따라 형식이 달라집니다. (1) **후보계정 선거비용외**는 기존 `자(비외)-1`에서 **괄호를 없앤 `자-1`**(후/보/외 동일)로 바뀝니다. 선거비용은 종전대로 `자(비)-1`을 유지합니다. (2) **후원회계정 지출**은 계정약자·괄호 없이 **지출 과목 약자만** 쓴 `기-1`(기부금)·`모-1`(후원금모금경비)·`인-1`(인건비)·`사-1`(사무소설치운영비)·`그-1`(그밖의경비) 형식으로 부여됩니다. 후원회 수입은 현행 표기를 유지합니다. 계정 계열은 `acc_sec_cd`(후보 자금원 82·83·84·85, 후원회 수입 1·지출 2) 하나로 판정하므로 일괄생성·백업 호출부는 변경되지 않았습니다. (미부여분만 채번·**기존 번호 보존**, 수입·지출 독립 채번, 과거 데이터 소급 재채번 없음)

## [0.14.4.0] - 2026-06-15

### Fixed

- **자료백업(.db)을 윈도우 선관위 프로그램에서 열 때 영수증일련번호가 자금원·과목별로 올바르게 표시됩니다.** 「영수증 일괄생성」을 실행하지 않았거나 번호가 없는 데이터를 가져온 상태로 백업하면, 백업 파일의 영수증번호(RCP_NO)가 비어 윈도우 프로그램이 정치자금 수입·지출부의 영수증일련번호를 **단일 항목(예: `자(비외)-1`~`자(비외)-11`)으로 잘못 묶어** 표시했습니다(실제로는 자금원·과목이 서로 다른 거래). 이제 자료백업 시 **증빙서첨부(`rcp_yn='Y'`)인데 번호가 비어 있는 거래**에 계정·과목 약자 규칙(`자(비외)`/`보(비)`/`자(비)` 등)으로 영수증번호를 자동 부여해, 자금원·과목별로 정확한 일련번호가 표시됩니다. (앱의 채번 SSOT 재사용, **기존 부여 번호는 보존**, 수입·지출 독립 채번, 통합본(full)·data1·data2 모든 백업 모드 적용)

## [0.14.3.0] - 2026-06-15

### Fixed

- **자료백업(.db)을 윈도우 선관위 프로그램에 올렸을 때 계정에 따라 수입·지출이 누락되던 문제를 고쳤습니다.** `data1`(후보자)/`data2`(후원회) 백업이 거래처(CUSTOMER)를 `org_id` 기준으로만 골라, 거래가 참조하는 **공유(org_id 없음)·타 기관 거래처**가 빠지면서 일부 거래가 거래처 없는 "고아" 행이 됐습니다. 선관위 프로그램은 정치자금 수입·지출부에서 거래처(수입제공자/지출받은자)를 연결할 때 이런 고아 행을 빼버려, 해당 거래처가 빠진 **계정의 수입(·지출)이 0으로** 표시됐습니다. 이제 백업이 **실제 거래가 참조하는 거래처를 모두 포함**하도록 바꿔, 계정별 수입·지출이 빠짐없이 표시됩니다. (실측: 한 후보 기관 data1 백업에서 후보자등자산 수입 14,602,392원·지출 41건이 누락→전액 복구. 통합본(full)·Fund_Master(master) 백업은 변화 없음, 익명 거래처 처리 유지)

## [0.14.2.0] - 2026-06-15

### Fixed

- **「정치자금 수입·지출부(보전 비용)」에 수입액이 반영되어 잔액이 정상화됩니다.** 직전 v0.14.0.0 양식이 지출(보전 선거비용)만 출력해 수입칸이 0으로 비고 잔액이 전부 음수로 나오던 문제를 수정했습니다. 이제 각 자금원(후보자등자산·후원회기부금·보조금·보조금외)의 **실제 수입 거래**를 날짜순으로 함께 표시하고, **잔액 = 수입누계 − 지출누계**로 계산합니다. 지출은 종전처럼 보전 최종청구액(`claim_amt`) 기준이라 **계정별·총 지출합계는 보전관리·보전청구서와 그대로 일치**합니다. (수입은 자금원 전체를 표시하므로 잔액은 선거비용외 지출이 빠진 보전청구 관점의 값이며, 실제 통장잔액과는 다를 수 있습니다. 영수증 첨부분/생략분은 지출만 집계, 수입 행 영수증 일련번호는 비움)

## [0.14.1.0] - 2026-06-15

### Changed

- **영수증번호 일괄생성이 계정·과목 약자 규칙으로 바뀝니다.** 「영수증일괄생성」(지출)·「영수증일괄입력」(수입) 시 기존 단순 순번(1, 2, 3…) 대신 **자금원 계정 약자(자/후/보/외) + 과목 약자(선거비용=비, 선거비용외=비외)**를 조합한 `자(비)-1` 형식으로 부여합니다. 같은 계정·과목 조합별로 1부터 매겨져 영수증 식별이 쉽고, 보전 수입·지출부의 일련번호 표기와 일치합니다. (지출·수입 공통 SSOT, 미부여분만 채번·기존 번호 보존, 정렬용 내부 번호는 유지)

## [0.14.0.0] - 2026-06-14

### Added

- **보전청구서에 첨부하는 「정치자금 수입·지출부(선거비용)」를 자동 생성합니다.** 보전비용 관리 → 보전청구서 탭의 **「수입·지출부(선거비용) 다운로드」** 버튼 한 번으로, 보전 체크된 선거비용 지출을 **수입계정(자금원)별 시트**로 나눈 공식 양식 .xlsx를 만듭니다. 지출액은 **보전 최종청구액**(일할계산·환급 반영, `claim_amt`)을 사용해 계정별 합계·총 합계가 보전관리·보전청구서·첨부서류목록과 **모두 일치**합니다. 계정명/과목명, 거래처(성명·사업자번호·주소·직업·전화), 영수증 일련번호(자금원 접두사 + 결제방법), 영수증 첨부분/생략분, 합계, 작성연월일·회계책임자 푸터까지 선관위 양식대로 채워집니다.

## [0.13.2.0] - 2026-06-14

### Fixed

- **보전 첨부서류목록(HWPX)도 환급(취소) 거래를 반영하도록 고쳤습니다.** 직전 v0.13.1.0에서 보전청구서·대시보드는 환급 차감을 반영했으나, 「선거비용 보전 첨부서류목록」 생성기에 동일한 누락(`acc_amt > 0` 필터)이 남아 있어 첨부서류목록의 항목 소계·합계가 과대 표시됐습니다. 이제 환급(음수) 거래가 해당 보전항목에서 차감되어, **첨부서류목록·보전청구서(서식43)·「선거비용 보전」 화면의 금액이 모두 일치**합니다. (일할계산 청구액은 기존에도 반영되고 있었고, 이번엔 환급 음수 누락만 수정)

## [0.13.1.0] - 2026-06-14

### Fixed

- **보전청구서 금액이 환급(취소) 거래를 반영하도록 고쳤습니다.** 선거비용 지출 중 환급받은 금액을 음수(−)로 입력한 경우(예: 문자발송 충전 후 미사용분 환급), 기존에는 보전청구서·보전 첨부서류목록·대시보드 보전 예상액 집계에서 이 환급분이 누락되어 청구액이 실제보다 크게 계산됐습니다. 이제 환급(음수) 거래가 보전 신청액에서 정확히 차감되어, 「선거비용 보전」 화면의 금액과 보전청구서(서식43·서식1) 금액이 일치합니다. (집계 게이트를 `acc_amt > 0` → `acc_amt ≠ 0`으로 변경, 0원 거래만 제외)

### Added

- **「선거비용 보전」 화면에 "보전 금액(청구 기준·전체)" 표시를 추가했습니다.** 조회 기간/계정과 무관하게 전체 보전 대상 합계를 항상 보여줘, 보전청구서에 실제로 들어갈 금액을 한눈에 확인할 수 있습니다(보전청구서·HWPX 서식43과 동일 집계 기준). 자금원이 4분류(후보자자산/후원회기부금/보조금/보조금외)로 분류되지 않는 거래가 있으면 경고로 표시해 청구 누락을 방지합니다.
- **보전 작업 중 데이터 유실을 방지합니다.** 보전 체크나 청구액(일할계산)을 수정한 뒤 저장하지 않고 다른 탭으로 이동하거나 다시 조회하면, 변경 내용이 사라지기 전에 경고합니다. 저장하지 않은 변경이 있으면 탭과 저장 버튼에 표시되고, 일부 저장 실패 시 사유를 안내합니다.

## [0.13.0.0] - 2026-06-13

### Added

- **보전 신청액(보전청구액)을 지출액과 따로 입력할 수 있게 했습니다.** 보전비용 중 일부 항목(예: 거리현수막을 40일 중 13일만 사용)은 **일할계산한 금액**으로만 보전 신청해야 합니다. 이제 「선거비용 보전」 화면의 지출 목록에서 **지출액 옆 「청구액(보전)」 칸**에 실제 신청할 금액을 직접 입력·수정할 수 있고(비워두면 지출액으로 청구), 저장하면 **선거비용 보전청구서(서식43)·보전 첨부서류목록·Excel 보전청구서**에 그 청구액이 자동 반영됩니다. 회계장부·회계보고서·정산 등 일반 회계 출력은 실제 지출액을 그대로 유지합니다. (DB: `acc_book.claim_amt` 컬럼 추가 — `scripts/015`)

## [0.12.0.0] - 2026-06-13

### Added

- **「선거비용 보전 첨부서류목록」을 회계 데이터로 자동 생성하는 기능을 추가했습니다.** 제출서류 화면에서 버튼 한 번으로, **보전 체크된 선거비용 지출**을 공직선거법 조항별 보전 항목(간판·현판·현수막 §61 / 거리게시용 현수막 §67 / 어깨띠 등 소품 §68 / 공개장소 연설·대담차량 §79 / 문자메시지 §82의5 / 명함 §93 / 선거벽보·선거공보 §64·65)으로 자동 분류하고, 항목별 거래업체·지출내용·보전청구액·첨부증빙 현황(사진/문서 매수·대표 파일명)을 담은 **점검목록표** .hwpx 를 생성합니다. 7개 항목 외 보전 체크 지출은 「기타/미분류」로 표기해 누락을 방지합니다. (후보자 회계 전용)

## [0.11.2.0] - 2026-06-11

### Fixed

- **후원회 수입 입력 시 「익명후원금」의 1회 10만원 한도 경고가 제대로 작동하도록 고쳤습니다.** 과목 코드가 선관위 공식 프로그램(PFund2)과 반대로 설정되어 있어, 익명후원금을 입력해도 한도 초과 경고가 뜨지 않고 엉뚱한 과목(그 밖의 수입)에 경고가 뜨던 문제가 있었습니다. 이제 과목 **이름**을 기준으로 판별하여 익명후원금 10만원 초과·기명후원금 30만원 초과 경고가 올바른 과목에만 표시됩니다. 과목 선택 시 내역 자동 입력도 정확해졌습니다. (이미 저장된 기록은 영향 없음)

## [0.11.1.0] - 2026-06-11

### Fixed

- **거래처를 선택하지 않고 수입·지출·수기입력을 저장할 때 발생하던 저장 실패를 고쳤습니다.** 거래처 없이 기록을 저장하면 저장되지 않던 문제가 있었는데, 이제 거래처를 비워두면 자동으로 **「익명」 거래처**로 기록됩니다. (신규 저장·수정·일괄 등록 모두 적용)

## [0.11.0.0] - 2026-06-11

### Added

- **「점자형 선거공보 등 부담비용 지급청구서」(서식 44)**를 데이터 채움으로 자동 생성합니다. 제출서류 화면에서 **"채워 받기" 버튼 한 번**으로 .hwpx 파일을 받을 수 있습니다.
  - 후보자명 등 조직 정보는 **자동으로 채워지고**, 선거명·소속정당명, 점자형 선거공보·선거공약서·저장매체의 **작성·제출 수량**, 항목별 **청구금액 표**(점자인쇄비·한글인쇄료·운반비·수당 등), **수령계좌**를 입력하면 공식 양식 그대로 완성됩니다.
  - 청구기한(선거일 후 10일) 내 제출용 — 활동보조인 수당·실비, 점자형 공보·공약서, 저장매체 비용 청구에 사용합니다.

### Fixed

- **제출서류 생성 시 입력값 안전성 강화** — 특수 제어문자가 섞인 값(웹·PDF 복사 시 흔함)으로 만든 파일이 한글에서 "손상된 파일"로 열리지 않던 문제를 예방하고, `{{` `}}` 같은 내부 예약 문자나 잘못된 형식의 값이 문서에 조용히 잘못 들어가는 경우를 입력 단계에서 차단합니다. (모든 HWPX 제출서류 공통 적용)

## [0.10.1.0] - 2026-06-11

### Fixed

- **선관위 제출파일(SQLite) 생성 오류 수정** — 거래 시각(`acc_time`) 입력 기능 추가 이후 자료백업·제출파일을 만들 때 `table ACC_BOOK has no column named ACC_TIME` 오류로 파일 생성이 전부 실패하던 문제를 고쳤습니다.
  - 거래 시각은 선관위 PFund2/SQLite 공식 포맷(`ACC_BOOK`)에 없는 앱 전용 항목이라, 제출파일을 만들 때 자동으로 제외해 공식 프로그램과 호환되도록 했습니다. (수입·지출 행 모두 적용)
  - 회계장부 입력·표시에는 거래 시각이 그대로 유지되며, 공식 제출용 파일에만 빠집니다.

## [0.10.0.0] - 2026-06-10

### Changed

- **「수입계정별 회계장부」(서식 7) HWPX**에서 **거래 내용이 없는 계정·과목도 빈 수입지출표로 빠짐없이 출력**합니다. 이전에는 거래가 있는 계정만 표가 만들어져 일부 계정이 누락됐으나, 이제 기관 유형의 표준 계정·과목 조합 전체(예: 후보자 = 4계정 × 2과목 = 8개 표)를 생성합니다.
  - 거래가 없는 계정은 **계정명·과목명 머리글 + 손기입용 빈 데이터행 1개**를 갖춘 양식으로 채워집니다.
  - 표 출력 순서는 선거관리위원회 표준 순서(`acc_rel`의 `acc_order` — 보조금 → 보조금외 → 후보자등자산 → 후원회기부금)를 따릅니다.

## [0.9.0.0] - 2026-06-10

### Added

- **「선거비용 보전청구서」(서식 43)**를 데이터 채움으로 자동 생성합니다. 제출서류 화면에서 회계보고서·회계장부와 동일하게 **"보전청구서 채워 받기" 버튼 한 번**으로 .hwpx 파일을 받을 수 있습니다.
  - **보전 체크(보전 대상)된 선거비용** 지출만 골라 **자금원별**(후보자자산·후원회기부금·정당의지원금)로 청구내역 표를 자동 집계합니다. (보조금·보조금외는 "정당의지원금"으로 합산)
  - 보전청구 총액을 **한글 금액 표기**("금○○○원")로 자동 변환하며, 이 표기는 선거비용 보전청구서 Excel과 동일한 기준을 씁니다.
  - 선거구명·수령계좌·관할 선거관리위원회 등 텍스트 항목은 화면에서 입력하면 표와 함께 채워집니다.
  - 자금원 분류·선거비용 판별 기준을 회계보고서(22-1·22-2)와 공유하여 **회계보고서 선거비용 합계와 보전청구액이 정합**합니다.
  - 현재는 전액을 **선거사무소** 열에 집계합니다(선거연락소 열은 빈칸). 선거연락소를 운영한 경우 양식 안내에 따라 사무소/연락소 분리분을 수기로 조정하면 됩니다.

## [0.8.0.0] - 2026-06-10

### Added

- (예비)후보자 회계보고서 **서식 22-2 「선거비용 지출내역 집계표」**를 데이터 채움으로 자동 생성합니다. 제출서류 화면에서 다른 회계보고서(22-1·22-3·22-4)와 동일하게 **"데이터 채움" 버튼 한 번**으로 .hwpx 파일을 받을 수 있습니다. 이로써 (예비)후보자 회계보고서 4종이 모두 자동 생성됩니다.
  - 지출 중 **선거비용만** 골라 **자금원 구분별**(후보자자산·후원회기부금·보조금·보조금외)로 집계해 표를 채웁니다.
  - 자금원 분류 기준을 수입·지출보고서(22-1)와 공유하여, **22-1 선거비용 합계와 22-2 합계가 항상 일치**합니다. (22-2에 없는 기타 자금원 선거비용은 보조금외 열에 합산해 총액을 보존)
  - 현재는 전액을 **선거사무소** 행에 집계합니다(선거연락소 행은 0). 선거연락소를 운영한 경우 양식 주석 안내에 따라 사무소/연락소 분리분을 수기로 조정하면 됩니다.

## [0.7.0.0] - 2026-06-09

### Added

- 수입·지출 내역을 입력할 때 **거래 시각(분 단위)**까지 기록할 수 있습니다. 수입·지출·서류등록(수기입력) 화면의 일자 옆에 시각 입력칸이 생겨, 같은 날 여러 건의 거래 순서와 실제 발생 시각을 남길 수 있습니다.
  - 시각은 **선택 입력**입니다. 비워두면 기존처럼 날짜만 저장됩니다.
  - 목록에는 기존과 동일하게 **날짜까지만** 표시됩니다. 같은 날짜 안에서는 시각이 입력된 건이 시간 순으로 정렬됩니다.
  - 선관위 제출용 파일(PFund2/SQLite, ACC_DATE) 형식과의 호환을 유지하기 위해 날짜(YYYYMMDD)는 그대로 두고, 시각은 별도 항목(`acc_time`, HHmm)으로 저장합니다.
  - 적용 전 DB 마이그레이션(`app/scripts/014_add_acc_time.sql`)을 Supabase에서 한 번 실행해야 합니다.

## [0.6.0.0] - 2026-06-08

### Added

- 지출내역 관리 화면에 **자금원별 충당 현황** 패널을 추가했습니다((예비)후보자 전용). 후보자자산·후원회기부금·보조금·보조금외 등 **수입계정(자금원)별로 받은 수입·지출한 금액·남은 가용잔액**을 사용기관 전체 누적 기준으로 한눈에 보여줍니다. 어느 수입계정에 여력이 남았는지 가늠해 지출을 할당하는 데 참고할 수 있습니다.
  - 가용잔액(수입−지출)이 음수인 자금원은 **초과충당 경고(⚠, 빨강)**로 표시합니다. 입력을 막지는 않습니다.
  - 집계는 결산(`computeBalances`)과 동일한 보정 규칙(마이너스 수입→지출 전환)을 재사용해 **결산 총액과 일치**합니다. 추정치이며 보전·확정 금액은 결산 기준입니다.
  - 추가 데이터 조회 없이 기존 화면 데이터를 재활용합니다.

## [0.5.0.0] - 2026-06-08

### Added

- (예비)후보자 **회계보고서 3종**(선관위 서식 22-1·22-3·22-4)을 빈 양식 대신 **입력된 데이터로 자동 채워** 한글파일(.hwpx)로 내려받을 수 있습니다. 제출서류 화면에서 서식을 선택하고 "데이터로 회계보고서 생성"을 누르면 됩니다.
  - **서식 22-1 수입·지출보고서**: 자금원 구분(자산·후원회기부금·정당지원금[보조금·보조금외])별로 수입과 지출(선거비용/선거비용외)을 집계하고 소계·잔액·합계를 자동 산출한 총괄표를 채웁니다.
  - **서식 22-3 재산명세서**: 재산 데이터를 구분(토지·건물·주식/유가증권·비품·현금/예금·그밖의재산)별 명세와 소계·합계로 채우며, 데이터가 없는 구분은 "해당없음"으로 양식 그대로 표기합니다.
  - **서식 22-4 수입·지출부**: 계정·과목별 수입·지출 상세를 거래상대방 정보(성명·생년월일·주소·직업·전화)와 함께 행으로 채웁니다.
  - 공식 양식 레이아웃(표·여백·폰트)을 그대로 보존합니다. (선거비용 지출내역 집계표인 서식 22-2는 이번 범위에서 제외)

### Changed

- HWPX 생성 코어에 `transformSection`을 추가해 템플릿 압축 해제를 1회로 줄이고, 토큰 치환 헬퍼(`replaceTokens`)를 회계장부·회계보고서 생성이 공유합니다(기존 서식 동작 불변).
- 재산 구분 코드(`ESTATE_TYPES`)를 공용 모듈로 분리해 재산관리 화면과 재산명세서 생성이 함께 사용합니다.

## [0.4.0.0] - 2026-06-08

### Added

- 선관위 제출서류 서식 7 "(예비)후보자 정치자금 수입계정별 회계장부(수입·지출부)"를 빈 양식 대신 **입력된 수입·지출 데이터로 자동 채워** 한글파일(.hwpx)로 내려받을 수 있습니다. 서식 7 선택 후 "수입 데이터로 회계장부 생성"을 누르면 계정·과목별로 정리된 공식 양식이 즉시 생성됩니다.
  - 계정·과목 조합마다 표를 나누고, **수입과 지출을 같은 표에 일자순으로 함께** 기록합니다(동일 자금원 기준).
  - 그룹 내 수입누계·지출누계와 **잔액(수입누계−지출누계)**을 자동 계산합니다.
  - 각 행에 연월일·내역·수입금액·지출금액·누계·잔액·거래상대방(성명·생년월일·주소·직업·전화)·영수증번호가 채워집니다.
  - 공식 form-7 레이아웃(표·여백·폰트)을 그대로 보존합니다.

### Changed

- 서식 입력 패널이 데이터 채움 서식(`dataFill`)을 인식해, 토큰 입력란 대신 데이터 생성 버튼을 노출합니다.
- HWPX 생성 코어에서 재패키징 로직(`repackageSection`)을 분리해 토큰 치환 서식과 회계장부 생성이 공유합니다(기존 서식 동작 불변).

## [0.3.0.0] - 2026-06-07

### Added

- 선관위 제출서류 메뉴에 공식 서식 24종을 다운로드용으로 추가(회계실무 작성예시 추출). 서식을 선택하면 한글(.hwpx) 빈 양식을 내려받아 직접 작성할 수 있습니다.
  - 회계장부: 후보자/후원회 수입계정별 회계장부, 회계장부 마감
  - 지급명세서: 선거사무관계자 수당·실비 지급명세서
  - 증빙정리: 영수증 등 증빙서류 첩부 및 정리
  - 회계보고서: (예비)후보자 회계보고서 4종, 후원회 회계보고서 11종
  - 정치자금영수증: 발급신청서, 발행·반납대장
  - 보전·청구: 선거비용 보전청구서, 점자형선거공보 등 부담비용 지급청구서
- 빈 양식 서식은 입력란 없이 안내문구와 함께 바로 다운로드되도록 입력 패널이 자동 전환됩니다.

### Changed

- 서식 카탈로그가 조직 타입(후보자/후원회)에 따라 노출 서식을 자동 필터링합니다.

## [0.2.0.0] - 2026-06-06

### Added

- 선관위 제출서류 HWPX 생성: 서식을 선택하면 사용기관 정보가 자동으로 채워지고, 빈 항목을 입력한 뒤 한글(.hwpx) 파일로 내려받을 수 있습니다. 제9회 지방선거 6종 서식 지원(정치자금 수입·지출 인계·인수서, 회계책임자 선임신고서·취임동의서, 예금계좌 신고서, 후원회 등록신청서·취임동의서).
- 조직 타입(후보자/후원회)에 따라 노출할 서식을 자동으로 필터링.
- 토큰 치환 HWPX 생성 코어: `{{토큰}}`을 입력값으로 치환하고 mimetype을 STORED(무압축) 첫 엔트리로 유지해 한글에서 정상적으로 열립니다. XML 특수문자 escape로 문서 파손·인젝션 방지.
- 서식 정의 ↔ 템플릿 토큰 정합성을 빌드타임 테스트로 검증(`_token-manifest.json` drift 가드 포함).

### Security

- HWPX 생성 API: 입력값 배열 거부, 필수 항목·입력 길이 서버 검증(쪽수 드리프트 + DoS 완화), 정의된 토큰만 화이트리스트 치환.

## [0.1.1.0] - 2026-04-05

### Added

- FAQ browser stays visible after clicking items, with a collapse/expand toggle so users can browse more questions while reading answers.
- Duplicate FAQ prevention: clicking a previously asked FAQ scrolls to the existing answer with a yellow highlight instead of adding a duplicate.
- FAQ messages tagged with `source: "faq"` to distinguish from manually typed questions, preventing false duplicate matches.
- Test infrastructure: vitest + @testing-library/react with 19 tests covering FAQ navigation, duplicate detection, collapse toggle, and chat input.

### Fixed

- FAQ collapse state now resets when clearing messages, preventing stale collapsed state after "대화 초기화".
- Highlight timeout properly cleaned up on component unmount via useRef, preventing resource leaks.

## [0.1.0] - Initial Release

- Political fund accounting management system with chatbot FAQ integration.
