# PDCA Archive — 2026-07

완료된 PDCA 사이클 문서 보관소.

| Feature | Match Rate | 완료일 | 문서 | 비고 |
|---------|:----------:|--------|------|------|
| [program-wide-review](program-wide-review/) | 95%(진단 커버리지) | 2026-07-06 | plan · analysis · report | 전체 프로그램 기능수행과정 리뷰 — 진단형 사이클(Design 생략, Check 선행형). 사용자 플로우 7개+횡단 축 4개를 병렬 조사 에이전트 6개로 전수 진단해 44건 발굴(P1 8/P2 20/P3 16) 후 심각도 순 실행. **보안(IDOR)**: service_role API 라우트 9개(acc-book·customers·excel-export·export-sqlite·import-sqlite·recompute-settlement·reimbursement-aggregate·evidence-file·workflow-status)에 공통 가드 `requireOrgMembership`(세션+`user_organ` 소속검증) 적용 — 로그인 없이 `?orgId=남의기관`으로 타 기관 재무데이터·거래처 PII·선관위 로그인 자격증명(export-sqlite) 접근하던 IDOR 차단. **버그**: 지출 거래처 미선택 저장 실패(BX1, API 경유 통일)·자료초기화 가짜 비밀번호 게이트(BX5)·감사 화면 열람 시 결산값 덮어쓰기(BX6)·익명 기부 집계 왜곡(B2, `-999` dead 조건→`anonymousCustIds` name 기반)·서식 입력값 소실(B1, prefill race→dirty 필드 추적)·옛 선거주기 잠금 삭제·영수증 우회(BX3)·결산 재산 합계 이원화(BX9, `estateAmount` SSOT). **DB 스키마**: 결산확정 후 수정 `SETTLED_PERIOD` 경고(BX7)+`finalize_settlement` RPC가 organ 회계기간을 덮어쓰던 것 제거(BX8, `scripts/023`). **백업**: import skip/merge 거래 중복적재 차단(BX4, overwrite만 교체)+overwrite 전역삭제를 대상 org로 스코프화(S4, 후속 PR). **성능**: 무제한 fetch truncation 방지(P-1)·batch_insert 일괄화 3N→~2+N/500(P-2)·요약 SUM RPC(P-3, `scripts/025`)·N+1 쓰기 제거(P-4). **기능(FR-07)**: export·backup·결산·reports 생성 시 회계기간 밖 거래 경고 표면화(차단 아님, 은폐 금지 방침). **데이터**: 익명 거래처 중복(65/117/244→정본 39) read-only 진단→dry-run→`--confirm` 실행 완료, 유니크 인덱스(`scripts/024`)는 수동 적용 대기. 회귀 테스트 900→945(+45). 2개 PR(#115 본편, #116 후속)로 분할 배포, 각 Vercel 게이트 통과 후 main 머지. **잔여(의도적 보류)**: HWPX 서식 FR-07(export·reports·결산이 이미 경고라 중복)·income 일괄삭제 N+1(API 경유 필요)·D-5/D-6/X3/X4 순수 정리. **⚠️ 배포 후 수동 적용 필요**: Supabase SQL 에디터에 `scripts/023`(결산확정 플래그)·`024`(익명 유니크, cleanup 실행됨)·`025`(요약 RPC) — 코드는 미적용 상태에서도 폴백/no-op으로 안전. (PR #115·#116 머지, v0.32.0.0→v0.32.1.0) |
